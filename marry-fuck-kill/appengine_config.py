# Uncomment this to enable stats (also uncomment URL handler in app.yaml):
#def webapp_add_wsgi_middleware(app):
#   from google.appengine.ext.appstats import recording
#   app = recording.appstats_wsgi_middleware(app)
#   return app
